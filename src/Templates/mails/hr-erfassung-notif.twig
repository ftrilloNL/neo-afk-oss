{{ 'mail.hr_erfassung.greeting'|trans({'%name%': target.display_name}) }}

{{ 'mail.hr_erfassung.body'|trans({'%art_descr%': (art == 'urlaub' ? 'mail.hr_erfassung.art.urlaub_descr' : 'mail.hr_erfassung.art.krank_descr')|trans}) }}

{{ 'mail.hr_erfassung.period_line'|trans({'%start%': startdatum|localized_date, '%end%': enddatum|localized_date}) }}
{{ 'mail.hr_erfassung.werktage_line'|trans({'%tage%': tage|number_format(1, ',', '.')}) }}
{% if notiz %}
{{ 'mail.hr_erfassung.notiz_line'|trans({'%notiz%': notiz}) }}
{% endif %}

{{ 'mail.hr_erfassung.closing'|trans }}

{{ org.name }}
